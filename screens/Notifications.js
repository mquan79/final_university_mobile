import { NotificationComponent } from '../components/NotificationComponent'
const Notifications = ({ title, message}) => {
  NotificationComponent(title, message);
  return null
}

export default Notifications